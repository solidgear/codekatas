function Engineer(androidExpert, iosExpert, frontendExpert, backendExpert) {
    this.androidExpert = androidExpert;
    this.iosExpert = iosExpert;
    this.frontendExpert = frontendExpert;
    this.backendExpert = backendExpert;
}

const percentageAsignedAndroid = 0.2;
const percentageAsignedIos = 0.25;
const percentageAsignedFrontend = 0.2;
const percentageAsignedBackend = 0.2;

function distributeEngineersInProjects(engineers) {
    // TO-DO
}