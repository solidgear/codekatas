- (void)fetchPolicies {
    // Fetching policies...

    // Converting from policies to actions...

    ActionManager *actionManager = [[ActionManager alloc] init];
    [actionManager performActions:actionsArray]
}